/**
 * Entry point for profile avatar styles.
 * Imports styles specific to profile avatar component.
 *
 * @since 2.0.0
 */
import './profile-avatar.scss';

/**
 * Entry point for sold-by label styles.
 * Imports styles specific to the sold-by label component.
 *
 * @since 2.0.0
 */
import './sold-by-label.scss';
